{
  "name": "apepatrol",
  "version": "3.4.19",
  "private": true,
  "description": "Firefox investigation companion for MaxPatrol SIEM",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint src scripts tests",
    "test": "node scripts/with-core.mjs vitest run",
    "build:raw": "node scripts/with-core.mjs node scripts/build.mjs",
    "verify:policy": "node scripts/verify-policy.mjs",
    "lint:firefox": "node scripts/lint-firefox.mjs",
    "build:firefox": "node scripts/with-core.mjs npm run build:firefox:resolved",
    "build:self-hosted": "node scripts/with-core.mjs npm run build:self-hosted:resolved",
    "package:firefox": "node scripts/with-core.mjs npm run package:firefox:resolved",
    "check:reproducible": "node scripts/with-core.mjs node scripts/check-reproducible.mjs",
    "benchmark:graph": "node scripts/with-core.mjs node scripts/benchmark-process-graph.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "core:check": "node scripts/with-core.mjs node scripts/check-core.mjs",
    "test:core": "node scripts/with-core.mjs node --test scripts/core-contracts.mjs scripts/core-resolution-contracts.mjs",
    "build:self-hosted:resolved": "npm run lint && npm test && node scripts/build.mjs --self-hosted && npm run verify:policy && npm run verify:release && node scripts/lint-firefox.mjs --self-hosted",
    "build:firefox:resolved": "npm run lint && npm test && node scripts/build.mjs && npm run verify:policy && npm run verify:release && npm run lint:firefox",
    "package:firefox:resolved": "npm run build:firefox && web-ext build --source-dir dist/firefox --artifacts-dir artifacts --filename apepatrol-{version}-firefox.zip --overwrite-dest",
    "core:prepare": "node scripts/with-core.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "eslint": "10.9.1",
    "globals": "17.11.0",
    "jsdom": "30.0.1",
    "vitest": "4.1.11",
    "web-ext": "10.6.0"
  },
  "apeShareCore": {
    "major": 1
  }
}
